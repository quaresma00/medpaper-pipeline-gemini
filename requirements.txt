# medpaper science dependencies
#
# The workflow driver (tools/wf.py) needs NONE of these - it is stdlib only, so gates keep
# working even when this environment is broken. These are for figures, tables and analysis.
#
# Versions are pinned to what the pipeline was verified against. Bump deliberately and
# re-run tools/selftest.py afterwards.

# figures + QC
matplotlib==3.11.1
numpy==2.5.2
pillow==12.3.0          # TIFF print masters (matplotlib reads PNG without it)

# tables
openpyxl==3.1.5
python-docx==1.2.0

# analysis
pandas==3.0.5
scipy==1.18.1           # also used by figure QC for grey-region labelling
statsmodels==0.15.0

# survival: required by the km_survival recipe. Do not hand-roll a product-limit estimator.
lifelines==0.30.0

# optional: force-directed label repulsion for volcano plots
# adjustText==1.3.0
